import { kategori, pro } from "../src/type-alias";
import { data } from "../src/type-alias";
import { sub } from "../src/type-alias";
const apalah: kategori = {
  id: "1",
  nama: "lana",
};

console.log(apalah);

const base: data = {
  id: 1,
  nama: "erine",
};

console.log(base);

const SuB: sub = {
  id: 13,
  nama: "lily",
};
console.log(SuB);
